name: CI

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Git Version
      run: git --version

    - name: Run a one-line script
      run: echo "Hello, World!"
